1. Factorial of a Number

👉 Write a function to calculate the factorial of a given number n.
(Hint: use loop or recursion)

2. Palindrome Checker

👉 Check if a given string is a palindrome (reads same forward and backward).
Example: madam → True, hello → False.
(Hint: compare string with string[::-1])

3. Sum of List Elements

👉 Find the sum of all numbers in a list.
Example: [10, 20, 30] → 60.
(Hint: use loop or built-in sum())

4. Find Maximum in a List

👉 Find the largest number in a list.
Example: [3, 8, 1, 5] → 8.
(Hint: use max() or loop)

5. Count Vowels in a String

👉 Count how many vowels (a, e, i, o, u) are in a string.
Example: "hello" → 2.

6. Reverse a String

👉 Reverse a given string without using slicing.
Example: "python" → "nohtyp".
(Hint: loop from end)

7. Even and Odd Numbers

👉 From a list, separate even and odd numbers into two new lists.
Example: [1,2,3,4,5] → evens=[2,4], odds=[1,3,5].

8. Dictionary – Word Count

👉 Count the frequency of each word in a sentence using a dictionary.
Example: "apple banana apple" → {'apple': 2, 'banana': 1}.
(Hint: use .get(key, 0))

9. Check Prime Number

👉 Write a program to check if a number is prime (only divisible by 1 and itself).
Example: 7 → Prime, 8 → Not Prime.

10. Fibonacci Series

👉 Print the first n numbers of the Fibonacci sequence.
Example for n=5: 0 1 1 2 3.