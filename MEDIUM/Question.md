Take user input and store it in a variable.

Take two inputs and store them in two different variables.

Convert user input from string to integer and store it.

Store the result of an arithmetic expression in a variable.

Find the memory address of a variable.

Check whether two variables have the same value.

Check whether two variables point to the same memory location.

Store the result of a comparison operation in a variable.

Create a variable and change its data type at runtime.

Assign values to multiple variables using unpacking.
